cal_crear = (
                  ( 'ESO', (
                        1, ('A','B','C','D',), 
                        2, ('A','B','C','D',),
                        ),
                  )
                  ( 'BTX', (
                        1, ('A','B',), 
                        2, ('A','B',),
                        ),
                  )
             )		

for x in cal_crear:
    x